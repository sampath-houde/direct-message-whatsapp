<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.codecat.directwhatsapp">

    <queries>
        <package android:name="com.whatsapp.w4b" />
        <package android:name="com.whatsapp" />
    </queries>

    <application
        android:icon="@mipmap/ic_launcher_round"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/BaseTheme">
        <activity
            android:name="com.codecat.directwhatsapp.PrivacyPolicyActivity"
            android:theme="@style/MainTheme"/>
        <activity
            android:name="com.codecat.directwhatsapp.CountrySelectorActivity"
            android:theme="@style/MainTheme" />
        <activity
            android:name="com.codecat.directwhatsapp.MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>